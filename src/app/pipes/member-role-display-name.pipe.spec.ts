import { MemberRoleDisplayNamePipe } from './member-role-display-name.pipe';

describe('MemberRoleDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberRoleDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
