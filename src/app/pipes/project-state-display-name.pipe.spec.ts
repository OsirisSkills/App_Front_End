import { ProjectStateDisplayNamePipe } from './project-state-display-name.pipe';

describe('ProjectStateDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectStateDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
